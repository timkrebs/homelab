#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  refresh-installer:
    update: no
  network:
    version: 2
    ethernets:
      ens18:
        dhcp4: true
        dhcp-identifier: mac
  identity:
    hostname: ubuntu-template
    username: packer
    password: "$6$packersa$p46UIkeTFQ4qHIRxrmU155wruG49wsi8K8Paw0bjOKQSeqhnMSpmeqWAy8UPZJHmHzYf2c0utbc4lQaQzlhek0"
  ssh:
    install-server: true
    allow-pw: true
  packages:
    - qemu-guest-agent
    - openssh-server
    - sudo
    - curl
    - wget
    - vim
    - htop
    - net-tools
    - jq
    - unzip
    - cloud-init
    - cloud-guest-utils
  storage:
    layout:
      name: direct
    swap:
      size: 0
  user-data:
    package_upgrade: false
    timezone: UTC
  late-commands:
    - curtin in-target --target=/target -- passwd -d packer
    - curtin in-target --target=/target -- sh -c 'echo "packer:packer" | chpasswd'
    - curtin in-target --target=/target -- sh -c 'echo "packer ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/packer'
    - curtin in-target --target=/target -- chmod 440 /etc/sudoers.d/packer
    - curtin in-target --target=/target -- systemctl enable qemu-guest-agent
    - curtin in-target --target=/target -- systemctl enable ssh
